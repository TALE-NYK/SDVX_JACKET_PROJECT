<p style="color: green"><%= notice %></p>

<%= # ここは大きくしたい %>
<%= render @character %>
<div>
  <%= render @tune_list %>
</div>

<div>
  <%= begin %>
    ここから編集はできないようにする
    <%= link_to "Edit this character", edit_character_path(@character) %> |
  <%= end %>

  <%= link_to "Back to Lists", characters_path %>

  <%= begin %>
    # 削除は基本的にできないようにする
    # <%= button_to "Destroy this character", @character, method: :delete %>
  <%= end %>
</div>
